import React from "react";
import { NavLink } from "react-router-dom";

export const Movie = () => {
  return (
    <div>
      Movie
      <NavLink to="/">Back to home page</NavLink>
    </div>
  );
};
